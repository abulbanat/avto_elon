import 'package:flutter/material.dart';

class SearchFavouritePage extends StatelessWidget {
  const SearchFavouritePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body:Center(
        child: Text("Search Favourite Page"),
      ),
    );
  }
}
