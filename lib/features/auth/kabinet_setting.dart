import 'package:flutter/material.dart';

class KabinetSetting extends StatelessWidget {
  const KabinetSetting({super.key});
  static const id = "kabinet_setting";

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
