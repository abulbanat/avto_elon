import 'package:flutter/material.dart';

class KabinetAdminLogin extends StatelessWidget {
  const KabinetAdminLogin({super.key});
  static const id = "kabinet_admin_login";

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
